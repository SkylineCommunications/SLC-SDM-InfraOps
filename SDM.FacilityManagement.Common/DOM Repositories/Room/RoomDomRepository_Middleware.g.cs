//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by the Skyline.DataMiner.SDM.CodeGenerator package.
//     Changes to this file will be lost if the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

namespace Skyline.DataMiner.SDM.FacilityManagement.Models
{
    using System;
    using System.Collections.Generic;
    using System.Linq;

    using Skyline.DataMiner.Net;
    using Skyline.DataMiner.Net.Apps.DataMinerObjectModel;
    using Skyline.DataMiner.Net.Apps.Sections.Sections;
    using Skyline.DataMiner.Net.Helper;
    using Skyline.DataMiner.Net.ManagerStore;
    using Skyline.DataMiner.Net.Messages;
    using Skyline.DataMiner.Net.Messages.SLDataGateway;
    using Skyline.DataMiner.Net.Sections;
    using Skyline.DataMiner.Net.SubscriptionFilters;
    using Skyline.DataMiner.SDM;

    using SLDataGateway.API.Querying;
    using SLDataGateway.API.Types.Querying;

    internal sealed partial class RoomDomRepository_Middleware : IBulkRepository<FacilityManagement.Models.Room>
    {
        private readonly IBulkRepository<FacilityManagement.Models.Room> _inner;
        private readonly IMiddlewareMarker<FacilityManagement.Models.Room> _middleware;

        public RoomDomRepository_Middleware(IBulkRepository<FacilityManagement.Models.Room> inner, IMiddlewareMarker<FacilityManagement.Models.Room> middleware)
        {
            _inner = inner ?? throw new ArgumentNullException(nameof(inner));
            _middleware = middleware;
        }

        public IEnumerable<IPagedResult<FacilityManagement.Models.Room>> ReadPaged(FilterElement<FacilityManagement.Models.Room> filter)
        {
            if (_middleware is IPageableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnReadPaged(filter, _inner.ReadPaged);
            }
            else
            {
                return _inner.ReadPaged(filter);
            }
        }

        public IEnumerable<IPagedResult<FacilityManagement.Models.Room>> ReadPaged(IQuery<FacilityManagement.Models.Room> query)
        {
            if (_middleware is IPageableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnReadPaged(query, _inner.ReadPaged);
            }
            else
            {
                return _inner.ReadPaged(query);
            }
        }

        public IEnumerable<IPagedResult<FacilityManagement.Models.Room>> ReadPaged(FilterElement<FacilityManagement.Models.Room> filter, int pageSize)
        {
            if (_middleware is IPageableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnReadPaged(filter, pageSize, _inner.ReadPaged);
            }
            else
            {
                return _inner.ReadPaged(filter, pageSize);
            }
        }

        public IEnumerable<IPagedResult<FacilityManagement.Models.Room>> ReadPaged(IQuery<FacilityManagement.Models.Room> query, int pageSize)
        {
            if (_middleware is IPageableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnReadPaged(query, pageSize, _inner.ReadPaged);
            }
            else
            {
                return _inner.ReadPaged(query, pageSize);
            }
        }

        public IEnumerable<FacilityManagement.Models.Room> Read(FilterElement<FacilityManagement.Models.Room> filter)
        {
            if (_middleware is IReadableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnRead(filter, _inner.Read);
            }
            else
            {
                return _inner.Read(filter);
            }
        }

        public IEnumerable<FacilityManagement.Models.Room> Read(IQuery<FacilityManagement.Models.Room> query)
        {
            if (_middleware is IReadableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnRead(query, _inner.Read);
            }
            else
            {
                return _inner.Read(query);
            }
        }

        public long Count(FilterElement<FacilityManagement.Models.Room> filter)
        {
            if (_middleware is ICountableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnCount(filter, _inner.Count);
            }
            else
            {
                return _inner.Count(filter);
            }
        }

        public long Count(IQuery<FacilityManagement.Models.Room> query)
        {
            if (_middleware is ICountableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnCount(query, _inner.Count);
            }
            else
            {
                return _inner.Count(query);
            }
        }

        public IReadOnlyCollection<FacilityManagement.Models.Room> Create(IEnumerable<FacilityManagement.Models.Room> oToCreate)
        {
            if (_middleware is IBulkCreatableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnCreate(oToCreate, _inner.Create);
            }
            else
            {
                return _inner.Create(oToCreate);
            }
        }

        public FacilityManagement.Models.Room Create(FacilityManagement.Models.Room oToCreate)
        {
            if (_middleware is ICreatableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnCreate(oToCreate, _inner.Create);
            }
            else
            {
                return _inner.Create(oToCreate);
            }
        }

        public IReadOnlyCollection<FacilityManagement.Models.Room> Update(IEnumerable<FacilityManagement.Models.Room> oToUpdate)
        {
            if (_middleware is IBulkUpdatableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnUpdate(oToUpdate, _inner.Update);
            }
            else
            {
                return _inner.Update(oToUpdate);
            }
        }

        public FacilityManagement.Models.Room Update(FacilityManagement.Models.Room oToUpdate)
        {
            if (_middleware is IUpdatableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                return middleware.OnUpdate(oToUpdate, _inner.Update);
            }
            else
            {
                return _inner.Update(oToUpdate);
            }
        }

        public void Delete(IEnumerable<FacilityManagement.Models.Room> oToDelete)
        {
            if (_middleware is IBulkDeletableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                middleware.OnDelete(oToDelete, _inner.Delete);
            }
            else
            {
                _inner.Delete(oToDelete);
            }
        }

        public void Delete(FacilityManagement.Models.Room oToDelete)
        {
            if (_middleware is IDeletableMiddleware<FacilityManagement.Models.Room> middleware)
            {
                middleware.OnDelete(oToDelete, _inner.Delete);
            }
            else
            {
                _inner.Delete(oToDelete);
            }
        }
        public System.Collections.Generic.IReadOnlyCollection<FacilityManagement.Models.Room> CreateOrUpdate(System.Collections.Generic.IEnumerable<FacilityManagement.Models.Room> oToCreateOrUpdate)
        {
                if (_middleware is IBulkRepositoryMiddleware<FacilityManagement.Models.Room> middleware)
                {
                    return middleware.OnCreateOrUpdate(oToCreateOrUpdate, _inner.CreateOrUpdate);
                }
                else
                {
                    return _inner.CreateOrUpdate(oToCreateOrUpdate);
                }
        }

    }
}
