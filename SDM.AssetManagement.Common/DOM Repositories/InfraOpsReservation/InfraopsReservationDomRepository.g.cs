//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by the Skyline.DataMiner.SDM.CodeGenerator package.
//     Changes to this file will be lost if the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
namespace Skyline.DataMiner.SDM.AssetManagement.Models
{
    using System;
    using System.Collections.Generic;
    using System.Linq;

    using Skyline.DataMiner.SDM.AssetManagement.Models;

    using Skyline.DataMiner.Net;
    using Skyline.DataMiner.Net.Apps.DataMinerObjectModel;
    using Skyline.DataMiner.Net.Apps.Sections.Sections;
    using Skyline.DataMiner.Net.Helper;
    using Skyline.DataMiner.Net.ManagerStore;
    using Skyline.DataMiner.Net.Messages.SLDataGateway;
    using Skyline.DataMiner.Net.Sections;
    using Skyline.DataMiner.Net.SubscriptionFilters;
    using Skyline.DataMiner.SDM;

    using SLDataGateway.API.Querying;
    using SLDataGateway.API.Types.Querying;

    internal partial class InfraopsReservationDomRepository : IBulkRepository<InfraopsReservation>
    {
        private readonly IConnection connection;
        private readonly DomHelper helper;
        public InfraopsReservationDomRepository(IConnection connection)
        {
            this.connection = connection;
            this.helper = new DomHelper(connection.HandleMessages, AssetManagement.Models.ReservationDomMapper.ModuleId);
        }

        public InfraopsReservation Create(InfraopsReservation createObject)
        {
            if (createObject is null)
            {
                throw new ArgumentNullException(nameof(createObject));
            }

            var instance = ToInstance(createObject);
            instance = helper.DomInstances.Create(instance);
            return FromInstance(instance);
        }

        public IReadOnlyCollection<InfraopsReservation> Create(IEnumerable<InfraopsReservation> createObjects)
        {
            if (createObjects is null || !createObjects.Any())
            {
                return Array.Empty<InfraopsReservation>();
            }

            // Check if some of the objects already exist
            var existing = new HashSet<string>();
            foreach (var batch in createObjects.Batch(500))
            {
                existing.UnionWith(Read(new ORFilterElement<InfraopsReservation>(batch.Select(obj => InfraopsReservationExposers.Identifier.Equal(obj.Identifier)).ToArray())).Select(obj => obj.Identifier));
            }

            // Create the remainder
            var SuccessfulItems = new List<InfraopsReservation>();
            var failures = new Dictionary<string, Exception>();
            var objects = createObjects.Where(obj => !existing.Contains(obj.Identifier)).ToDictionary(obj => obj.Identifier);
            foreach (var batch in createObjects.Select(ToInstance).Batch(helper.DomInstances.MaxAmountBulkOperation))
            {
                helper.DomInstances.TryCreateOrUpdate(batch.ToList(), out var result);
                foreach (var failure in result.UnsuccessfulIds)
                {
                    failures.Add(failure.Id.ToString(), new CrudFailedException(result.TraceDataPerItem[failure]));
                }

                foreach (var success in result.SuccessfulItems)
                {
                    SuccessfulItems.Add(FromInstance(success));
                }
            }

            // If everything went fine, return the successful creations
            if (!existing.Any() && !failures.Any())
            {
                return SuccessfulItems;
            }

            // Otherwise, build and throw an exception
            var exceptionBuilder = new SdmBulkCrudException<InfraopsReservation>.Builder();
            foreach (var obj in createObjects)
            {
                if (existing.Contains(obj.Identifier))
                {
                    exceptionBuilder.AddFailed(obj, new SdmCrudException<InfraopsReservation>(obj, $"Could not create Reservation with guid: '{obj.Identifier}', it already exists."));
                    continue;
                }

                if (failures.ContainsKey(obj.Identifier))
                {
                    exceptionBuilder.AddFailed(obj, failures[obj.Identifier]);
                    continue;
                }

                exceptionBuilder.AddSuccessful(obj);
            }

            throw exceptionBuilder.Build();
        }

        public IReadOnlyCollection<InfraopsReservation> CreateOrUpdate(IEnumerable<InfraopsReservation> items)
        {
            if (items is null || !items.Any())
            {
                return Array.Empty<InfraopsReservation>();
            }

            var successful = new List<InfraopsReservation>();
            var exceptionBuilder = new SdmBulkCrudException<InfraopsReservation>.Builder();
            var objects = items.ToDictionary(obj => obj.Identifier);
            foreach (var batch in items.Select(ToInstance).Batch(helper.DomInstances.MaxAmountBulkOperation))
            {
                helper.DomInstances.TryCreateOrUpdate(batch.ToList(), out var result);
                foreach (var failure in result.UnsuccessfulIds)
                {
                    exceptionBuilder.AddFailed(objects[failure.Id.ToString()], new CrudFailedException(result.TraceDataPerItem[failure]));
                }

                foreach (var success in result.SuccessfulItems)
                {
                    var item = FromInstance(success);
                    exceptionBuilder.AddSuccessful(item);
                    successful.Add(item);
                }
            }

            if (exceptionBuilder.HasFailure)
            {
                throw exceptionBuilder.Build();
            }

            return successful;
        }

        public long Count(FilterElement<InfraopsReservation> filter)
        {
            if (filter is null)
            {
                throw new ArgumentNullException(nameof(filter));
            }

            var domFilter = TranslateFullFilter(filter);
            domFilter = domFilter.AND(DomInstanceExposers.DomDefinitionId.Equal(AssetManagement.Models.ReservationDomMapper.DomDefinitionId.Id));
            return helper.DomInstances.Count(domFilter);
        }

        public long Count(IQuery<InfraopsReservation> query)
        {
            if (query is null)
            {
                throw new ArgumentNullException(nameof(query));
            }

            var domFilter = TranslateFullFilter(query.Filter);
            domFilter = domFilter.AND(DomInstanceExposers.DomDefinitionId.Equal(AssetManagement.Models.ReservationDomMapper.DomDefinitionId.Id));
            var domOrder = TranslateFullOrderBy(query.Order);
            var domQuery = query.WithFilter(domFilter).WithOrder(domOrder);
            return helper.DomInstances.Count(domQuery);
        }

        public IEnumerable<InfraopsReservation> Read(FilterElement<InfraopsReservation> filter)
        {
            if (filter is null)
            {
                throw new ArgumentNullException(nameof(filter));
            }

            var domFilter = TranslateFullFilter(filter);
            return Read(domFilter);
        }

        public IEnumerable<InfraopsReservation> Read(IQuery<InfraopsReservation> query)
        {
            if (query is null)
            {
                throw new ArgumentNullException(nameof(query));
            }

            var domFilter = TranslateFullFilter(query.Filter);
            var domOrder = TranslateFullOrderBy(query.Order);
            var domQuery = query.WithFilter(domFilter).WithOrder(domOrder);
            return Read(domQuery);
        }

        public IEnumerable<IPagedResult<InfraopsReservation>> ReadPaged(FilterElement<InfraopsReservation> filter)
        {
            return ReadPaged(filter, 500);
        }

        public IEnumerable<IPagedResult<InfraopsReservation>> ReadPaged(FilterElement<InfraopsReservation> filter, int pageSize)
        {
            if (filter is null)
            {
                throw new ArgumentNullException(nameof(filter));
            }

            if (pageSize <= 0)
            {
                throw new ArgumentOutOfRangeException(nameof(pageSize), "The page size must be 1 or higher");
            }

            var domFilter = TranslateFullFilter(filter);
            var paging = ReadPaged(domFilter, pageSize).GetEnumerator();
            var moveNext = paging.MoveNext();
            var i = 0;
            while (moveNext)
            {
                var page = paging.Current.ToList();
                moveNext = paging.MoveNext();
                var result = new PagedResult<InfraopsReservation>(page, i, pageSize, moveNext);
                yield return result;
                i++;
            }
        }

        public IEnumerable<IPagedResult<InfraopsReservation>> ReadPaged(IQuery<InfraopsReservation> query)
        {
            return ReadPaged(query, 500);
        }

        public IEnumerable<IPagedResult<InfraopsReservation>> ReadPaged(IQuery<InfraopsReservation> query, int pageSize)
        {
            if (query is null)
            {
                throw new ArgumentNullException(nameof(query));
            }

            if (pageSize <= 0)
            {
                throw new ArgumentOutOfRangeException(nameof(pageSize), "The page size must be 1 or higher");
            }

            var domFilter = TranslateFullFilter(query.Filter);
            var domOrder = TranslateFullOrderBy(query.Order);
            var domQuery = query.WithFilter(domFilter).WithOrder(domOrder);
            var paging = ReadPaged(domQuery, pageSize).GetEnumerator();
            var moveNext = paging.MoveNext();
            var i = 0;
            while (moveNext)
            {
                var page = paging.Current.ToList();
                moveNext = paging.MoveNext();
                var result = new PagedResult<InfraopsReservation>(page, i, pageSize, moveNext);
                yield return result;
                i++;
            }
        }

        public InfraopsReservation Update(InfraopsReservation updateObject)
        {
            if (updateObject is null)
            {
                throw new ArgumentNullException(nameof(updateObject));
            }

            var instance = ToInstance(updateObject);
            instance = helper.DomInstances.Update(instance);
            return FromInstance(instance);
        }

        public IReadOnlyCollection<InfraopsReservation> Update(IEnumerable<InfraopsReservation> updateObjects)
        {
            if (updateObjects is null || !updateObjects.Any())
            {
                return Array.Empty<InfraopsReservation>();
            }

            // Check if which objects already exist
            var existing = new HashSet<string>();
            foreach (var batch in updateObjects.Batch(500))
            {
                existing.UnionWith(Read(new ORFilterElement<InfraopsReservation>(batch.Select(obj => InfraopsReservationExposers.Identifier.Equal(obj.Identifier)).ToArray())).Select(obj => obj.Identifier));
            }

            // Update the existing objects
            var successfulItems = new List<InfraopsReservation>();
            var failures = new Dictionary<string, Exception>();
            var objects = updateObjects.Where(obj => existing.Contains(obj.Identifier)).ToDictionary(obj => obj.Identifier);
            foreach (var batch in updateObjects.Select(ToInstance).Batch(helper.DomInstances.MaxAmountBulkOperation))
            {
                helper.DomInstances.TryCreateOrUpdate(batch.ToList(), out var result);
                foreach (var failure in result.UnsuccessfulIds)
                {
                    failures.Add(failure.Id.ToString(), new CrudFailedException(result.TraceDataPerItem[failure]));
                }

                foreach (var success in result.SuccessfulItems)
                {
                    successfulItems.Add(FromInstance(success));
                }
            }

            // Check for failures and build exception if needed
            var exceptionBuilder = new SdmBulkCrudException<InfraopsReservation>.Builder();
            foreach (var obj in updateObjects)
            {
                if (!existing.Contains(obj.Identifier))
                {
                    exceptionBuilder.AddFailed(obj, new SdmCrudException<InfraopsReservation>(obj, "Could not update a non existing Reservation"));
                    continue;
                }

                if (failures.ContainsKey(obj.Identifier))
                {
                    exceptionBuilder.AddFailed(obj, failures[obj.Identifier]);
                    continue;
                }

                exceptionBuilder.AddSuccessful(obj);
            }

            if (exceptionBuilder.HasFailure)
            {
                throw exceptionBuilder.Build();
            }

            return successfulItems;
        }

        public void Delete(InfraopsReservation deleteObject)
        {
            if (deleteObject is null)
            {
                throw new ArgumentNullException(nameof(deleteObject));
            }

            var instance = ToInstance(deleteObject);
            helper.DomInstances.Delete(instance);
        }

        public void Delete(IEnumerable<InfraopsReservation> deleteObjects)
        {
            if (deleteObjects is null || !deleteObjects.Any())
            {
                return;
            }

            var exceptionBuilder = new SdmBulkCrudException<InfraopsReservation>.Builder();
            var objects = deleteObjects.ToDictionary(obj => obj.Identifier);
            foreach (var batch in deleteObjects.Select(ToInstance).Batch(helper.DomInstances.MaxAmountBulkOperation))
            {
                helper.DomInstances.TryDelete(batch.ToList(), out var result);
                foreach (var failure in result.UnsuccessfulIds)
                {
                    exceptionBuilder.AddFailed(objects[failure.Id.ToString()], new CrudFailedException(result.TraceDataPerItem[failure]));
                }

                foreach (var success in result.SuccessfulIds)
                {
                    exceptionBuilder.AddSuccessful(objects[success.Id.ToString()]);
                }
            }

            if (exceptionBuilder.HasFailure)
            {
                throw exceptionBuilder.Build();
            }
        }

        private IEnumerable<InfraopsReservation> Read(FilterElement<DomInstance> domFilter)
        {
            if (domFilter is null)
            {
                throw new ArgumentNullException(nameof(domFilter));
            }

            domFilter = domFilter.AND(DomInstanceExposers.DomDefinitionId.Equal(AssetManagement.Models.ReservationDomMapper.DomDefinitionId.Id));
            var domInstances = helper.DomInstances.Read(domFilter);
            return domInstances.Select(FromInstance);
        }

        private IEnumerable<InfraopsReservation> Read(IQuery<DomInstance> domQuery)
        {
            if (domQuery is null)
            {
                throw new ArgumentNullException(nameof(domQuery));
            }

            var domFilter = domQuery.Filter.AND(DomInstanceExposers.DomDefinitionId.Equal(AssetManagement.Models.ReservationDomMapper.DomDefinitionId.Id));
            domQuery = domQuery.WithFilter(domFilter);
            var domInstances = helper.DomInstances.Read(domQuery);
            return domInstances.Select(FromInstance);
        }

        private IEnumerable<IEnumerable<InfraopsReservation>> ReadPaged(FilterElement<DomInstance> domFilter, int pageSize)
        {
            if (domFilter is null)
            {
                throw new ArgumentNullException(nameof(domFilter));
            }

            domFilter = domFilter.AND(DomInstanceExposers.DomDefinitionId.Equal(AssetManagement.Models.ReservationDomMapper.DomDefinitionId.Id));
            var pagingHelper = helper.DomInstances.PreparePaging(domFilter, pageSize);
            while (pagingHelper.MoveToNextPage())
            {
                yield return pagingHelper.GetCurrentPage().Select(FromInstance);
            }
        }

        private IEnumerable<IEnumerable<InfraopsReservation>> ReadPaged(IQuery<DomInstance> domQuery, int pageSize)
        {
            if (domQuery is null)
            {
                throw new ArgumentNullException(nameof(domQuery));
            }

            var domFilter = domQuery.Filter.AND(DomInstanceExposers.DomDefinitionId.Equal(AssetManagement.Models.ReservationDomMapper.DomDefinitionId.Id));
            domQuery = domQuery.WithFilter(domFilter);
            var pagingHelper = helper.DomInstances.PreparePaging(domQuery, pageSize);
            while (pagingHelper.MoveToNextPage())
            {
                yield return pagingHelper.GetCurrentPage().Select(FromInstance);
            }
        }

        private FilterElement<DomInstance> TranslateFullFilter(FilterElement<InfraopsReservation> filter)
        {
            if (filter is null)
            {
                throw new ArgumentNullException(nameof(filter));
            }

            FilterElement<DomInstance> translated;
            if (filter is ANDFilterElement<InfraopsReservation> and)
            {
                translated = new ANDFilterElement<DomInstance>(and.subFilters.Select(TranslateFullFilter).ToArray());
            }
            else if (filter is ORFilterElement<InfraopsReservation> or)
            {
                translated = new ORFilterElement<DomInstance>(or.subFilters.Select(TranslateFullFilter).ToArray());
            }
            else if (filter is NOTFilterElement<InfraopsReservation> not)
            {
                translated = new NOTFilterElement<DomInstance>(TranslateFullFilter(not));
            }
            else if (filter is TRUEFilterElement<InfraopsReservation>)
            {
                translated = new TRUEFilterElement<DomInstance>();
            }
            else if (filter is FALSEFilterElement<InfraopsReservation>)
            {
                translated = new FALSEFilterElement<DomInstance>();
            }
            else if (filter is ManagedFilterIdentifier managedFilter)
            {
                translated = TranslateFilter(managedFilter);
            }
            else
            {
                throw new NotSupportedException($"Unsupported filter: {filter}");
            }

            return translated;
        }

        private IOrderBy TranslateFullOrderBy(IOrderBy order)
        {
            if (order is null)
            {
                throw new ArgumentNullException(nameof(order));
            }

            var translatedElements = new List<IOrderByElement>();
            foreach (var orderByElement in order.Elements)
            {
                var translated = TranslateOrderBy(orderByElement);
                translatedElements.Add(translated);
            }

            return new OrderBy(translatedElements);
        }

        private FilterElement<DomInstance> TranslateFilter(ManagedFilterIdentifier managedFilter)
        {
            if (managedFilter is null)
            {
                throw new ArgumentNullException(nameof(managedFilter));
            }

            var fieldName = managedFilter.getFieldName().fieldName;
            var comparer = managedFilter.getComparer();
            var value = managedFilter.getValue();
            var translated = CreateFilter(fieldName, comparer, value);
            return translated;
        }

        private IOrderByElement TranslateOrderBy(IOrderByElement orderByElement)
        {
            if (orderByElement is null)
            {
                throw new ArgumentNullException(nameof(orderByElement));
            }

            var fieldName = orderByElement.Exposer.fieldName;
            var sortOrder = orderByElement.SortOrder;
            var naturalSort = orderByElement.Options.NaturalSort;
            var translated = CreateOrderBy(fieldName, sortOrder, naturalSort);
            return translated;
        }

        private InfraopsReservation FromInstance(DomInstance instance)
        {
            var obj = new InfraopsReservation
            {
                Identifier = instance.ID.Id.ToString()
            };
            var _reservationpropertiesSection = instance.Sections.FirstOrDefault(s => s.SectionDefinitionID.Equals(AssetManagement.Models.ReservationDomMapper.ReservationProperties.SectionDefinitionId));
            if (_reservationpropertiesSection != default)
            {
                obj.ReservationPropertiesSectionId = _reservationpropertiesSection.ID.Id;
                var _description = _reservationpropertiesSection.GetValue<string>(AssetManagement.Models.ReservationDomMapper.ReservationProperties.Description);
                if (_description != null)
                {
                    obj.Description = _description.Value;
                }
            }

            var _rackfkSection = instance.Sections.FirstOrDefault(s => s.SectionDefinitionID.Equals(AssetManagement.Models.ReservationDomMapper.RackFk.SectionDefinitionId));
            if (_rackfkSection != default)
            {
                obj.RackFk = new AssetManagement.Models.RackRelation();
                ((Skyline.DataMiner.Utils.InfraOps.Common.Fields.ISectionTrackable)obj.RackFk).SectionId = _rackfkSection.ID.Id;
                var _rackfkrack = _rackfkSection.GetValue<System.Guid>(AssetManagement.Models.ReservationDomMapper.RackFk.Rack);
                if (_rackfkrack != null)
                {
                    obj.RackFk.Rack = new SdmObjectReference<FacilityManagement.Models.Rack>(Convert.ToString(_rackfkrack.Value));
                }
            }

            var _reservedpositionsList = new System.Collections.Generic.List<AssetManagement.Models.InfraopsReservationBounderies>();
            foreach (var _reservedpositionsSection in instance.Sections.Where(s => s.SectionDefinitionID.Equals(AssetManagement.Models.ReservationDomMapper.ReservedPositions.SectionDefinitionId)))
            {
                var reservedpositions = new AssetManagement.Models.InfraopsReservationBounderies();
                ((Skyline.DataMiner.Utils.InfraOps.Common.Fields.ISectionTrackable)reservedpositions).SectionId = _reservedpositionsSection.ID.Id;
                var _reservedpositionslowerbound = _reservedpositionsSection.GetValue<long>(AssetManagement.Models.ReservationDomMapper.ReservedPositions.LowerBound);
                if (_reservedpositionslowerbound != null)
                {
                    reservedpositions.LowerBound = _reservedpositionslowerbound.Value;
                }

                var _reservedpositionsupperbound = _reservedpositionsSection.GetValue<long>(AssetManagement.Models.ReservationDomMapper.ReservedPositions.UpperBound);
                if (_reservedpositionsupperbound != null)
                {
                    reservedpositions.UpperBound = _reservedpositionsupperbound.Value;
                }

                _reservedpositionsList.Add(reservedpositions);
            }

            obj.ReservedPositions = _reservedpositionsList;
            return obj;
        }

        private DomInstance ToInstance(InfraopsReservation obj)
        {
            Guid id = default(Guid);
            if (!String.IsNullOrEmpty(obj.Identifier))
            {
                id = Guid.Parse(obj.Identifier);
            }
            else
            {
                id = Guid.NewGuid();
            }

            var instance = new DomInstance
            {
                DomDefinitionId = AssetManagement.Models.ReservationDomMapper.DomDefinitionId,
                ID = new DomInstanceId(id)
                {
                    ModuleId = AssetManagement.Models.ReservationDomMapper.ModuleId
                }
            };
            var _reservationproperties = new Section(AssetManagement.Models.ReservationDomMapper.ReservationProperties.SectionDefinitionId);
            if (obj.ReservationPropertiesSectionId.HasValue)
            {
                _reservationproperties.ID = new SectionID(obj.ReservationPropertiesSectionId.Value);
            }
            if (obj.Description != default)
            {
                _reservationproperties.AddOrUpdateValue<string>(AssetManagement.Models.ReservationDomMapper.ReservationProperties.Description, Convert.ToString(obj.Description));
            }

            instance.Sections.Add(_reservationproperties);
            if (obj.RackFk != null)
            {
                var _rackfk = new Section(AssetManagement.Models.ReservationDomMapper.RackFk.SectionDefinitionId);
                var _rackfkSectionId = ((Skyline.DataMiner.Utils.InfraOps.Common.Fields.ISectionTrackable)obj.RackFk).SectionId;
                if (_rackfkSectionId.HasValue)
                {
                    _rackfk.ID = new SectionID(_rackfkSectionId.Value);
                }
                if (obj.RackFk.Rack != default && System.Guid.TryParse(obj.RackFk.Rack.Identifier, out var rackGuid) && rackGuid != System.Guid.Empty)
                {
                    _rackfk.AddOrUpdateValue<System.Guid>(AssetManagement.Models.ReservationDomMapper.RackFk.Rack, rackGuid);
                }

                instance.Sections.Add(_rackfk);
            }

            foreach (var reservedpositions in obj.ReservedPositions)
            {
                var _reservedpositionsSection = new Section(AssetManagement.Models.ReservationDomMapper.ReservedPositions.SectionDefinitionId);
                var _reservedpositionsSectionId = ((Skyline.DataMiner.Utils.InfraOps.Common.Fields.ISectionTrackable)reservedpositions).SectionId;
                if (_reservedpositionsSectionId.HasValue)
                {
                    _reservedpositionsSection.ID = new SectionID(_reservedpositionsSectionId.Value);
                }
                if (reservedpositions.LowerBound != default)
                {
                    _reservedpositionsSection.AddOrUpdateValue<long>(AssetManagement.Models.ReservationDomMapper.ReservedPositions.LowerBound, (long)reservedpositions.LowerBound);
                }

                if (reservedpositions.UpperBound != default)
                {
                    _reservedpositionsSection.AddOrUpdateValue<long>(AssetManagement.Models.ReservationDomMapper.ReservedPositions.UpperBound, (long)reservedpositions.UpperBound);
                }

                instance.Sections.Add(_reservedpositionsSection);
            }

            return instance;
        }

        private FilterElement<DomInstance> CreateFilter(string fieldName, Comparer comparer, object value)
        {
            switch (fieldName)
            {
                case "Identifier":
                    return FilterElementFactory.Create<DomInstance>(DomInstanceExposers.Id, comparer, Guid.Parse((string)value));
                case "Description":
                    return new DynamicManagedListFilter<DomInstance, object>(DomInstanceExposers.FieldValues.DomInstanceField(AssetManagement.Models.ReservationDomMapper.ReservationProperties.Description), comparer, (string)value);
                case "RackFk.Rack":
                    return new DynamicManagedListFilter<DomInstance, object>(DomInstanceExposers.FieldValues.DomInstanceField(AssetManagement.Models.ReservationDomMapper.RackFk.Rack), comparer, System.Guid.Parse(SdmObjectReference<FacilityManagement.Models.Rack>.Convert(value).Identifier));
                case "ReservedPositions.LowerBound":
                    return new DynamicManagedListFilter<DomInstance, object>(DomInstanceExposers.FieldValues.DomInstanceField(AssetManagement.Models.ReservationDomMapper.ReservedPositions.LowerBound), comparer, (long)value);
                case "ReservedPositions.UpperBound":
                    return new DynamicManagedListFilter<DomInstance, object>(DomInstanceExposers.FieldValues.DomInstanceField(AssetManagement.Models.ReservationDomMapper.ReservedPositions.UpperBound), comparer, (long)value);
                default:
                    throw new NotImplementedException();
            }
        }

        private IOrderByElement CreateOrderBy(string fieldName, SortOrder sortOrder, bool naturalSort = false)
        {
            switch (fieldName)
            {
                case "Identifier":
                    return OrderByElementFactory.Create(DomInstanceExposers.Id, sortOrder, naturalSort);
                case "Description":
                    return OrderByElementFactory.Create(DomInstanceExposers.FieldValues.DomInstanceField(AssetManagement.Models.ReservationDomMapper.ReservationProperties.Description), sortOrder, naturalSort);
                case "RackFk.Rack":
                    return OrderByElementFactory.Create(DomInstanceExposers.FieldValues.DomInstanceField(AssetManagement.Models.ReservationDomMapper.RackFk.Rack), sortOrder, naturalSort);
                case "ReservedPositions.LowerBound":
                    return OrderByElementFactory.Create(DomInstanceExposers.FieldValues.DomInstanceField(AssetManagement.Models.ReservationDomMapper.ReservedPositions.LowerBound), sortOrder, naturalSort);
                case "ReservedPositions.UpperBound":
                    return OrderByElementFactory.Create(DomInstanceExposers.FieldValues.DomInstanceField(AssetManagement.Models.ReservationDomMapper.ReservedPositions.UpperBound), sortOrder, naturalSort);
                default:
                    throw new NotImplementedException();
            }
        }
    }
}